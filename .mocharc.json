{
    "$schema": "https://json.schemastore.org/mocharc",
    "extensions": ["ts"],
    "spec": "src/test/typescript/**/*.test.ts",
    "recursive": true,
    "require": [
        "ts-node/register",
        "tsconfig-paths/register"
    ]
}